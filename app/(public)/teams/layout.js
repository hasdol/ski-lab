/**
 * SEO: Public teams overview — indexable.
 * @type {import('next').Metadata}
 */
export const metadata = {
  title: 'Teams | Ski Lab',
  description: 'Explore ski testing teams and join a team to manage ski testing with your group.',
};

export default function TeamsLayout({ children }) {
  return <>{children}</>;
}
