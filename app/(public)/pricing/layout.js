/**
 * SEO: Pricing — indexable and descriptive.
 * @type {import('next').Metadata}
 */
export const metadata = {
  title: 'Pricing | Ski Lab',
  description: 'See Ski Lab subscription plans and pricing for athletes, coaches, and companies.',
};

export default function PricingLayout({ children }) {
  return <>{children}</>;
}