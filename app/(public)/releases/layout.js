/**
 * SEO: Releases / changelog — indexable.
 * @type {import('next').Metadata}
 */
export const metadata = {
  title: 'Releases | Ski Lab',
  description: 'What’s new in Ski Lab — release notes and product updates.',
};

export default function ReleasesLayout({ children }) {
  return <>{children}</>;
}